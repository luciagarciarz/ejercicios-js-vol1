/*
    1.Crea una variable llamada pasos con valor inicial 0.
    2. Simula que el usuario da 3 pasos, usando el operador ++ cada vez.
    3. Muestra el valor de pasos después de cada incremento
*/

let pasos = 0;
pasos++;
console.log(pasos);

pasos++;
console.log(pasos);

pasos++;
console.log(pasos);